class QuoteSubscriptionParameters {
  final dynamic socketConnection;
  final int instrumentIdentifier;

  QuoteSubscriptionParameters({
    required this.socketConnection,
    required this.instrumentIdentifier,
  });
}
